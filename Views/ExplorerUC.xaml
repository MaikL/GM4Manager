<UserControl x:Class="GM4ManagerWPF.Views.ExplorerUC"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			xmlns:local="clr-namespace:GM4ManagerWPF.Localization"
            xmlns:conv="clr-namespace:GM4ManagerWPF.Converters"
			xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:viewmodels="clr-namespace:GM4ManagerWPF.ViewModels"
            xmlns:ui="http://schemas.modernwpf.com/2019"            
			mc:Ignorable="d"            
			d:DesignHeight="700" d:DesignWidth="900">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="25*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Label Content="{Binding Res[lblNetworkShare]}" HorizontalAlignment="Center" VerticalAlignment="Center" Height="40" Padding="0,5,0,5" />
        <StackPanel Grid.Column="1" Grid.ColumnSpan="3" Orientation="Horizontal">
            <TextBox Height="40" Text="{Binding SelectedPath, UpdateSourceTrigger=PropertyChanged}" 
                IsReadOnly="True" Margin="0,5,0,10" Width="300" IsReadOnlyCaretVisible="True" />
            <Button Height="40" Margin="10,5,0,10" Content="{Binding Res[txtPlaseSelectNetworkDirectory]}"
                Command="{Binding OpenFolderDialogCommand}" />
        </StackPanel>
        <TreeView x:Name="DirectoryTree" Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="0" ItemsSource="{Binding RootItems}"
                  SelectedItemChanged="DirectoryTree_SelectedItemChanged">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <!-- CRUCIAL BINDING -->
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
        <DataGrid Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2"
          ItemsSource="{Binding CurrentPermissions}" 
          x:Name="PermissionsGrid"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="True">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{Binding Res[colEditRights]}" 
                      Command="{Binding EditPermissionCommand}" 
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    <MenuItem Header="{Binding Res[colRemoveUser]}" 
                      Command="{Binding RemovePermissionCommand}" 
                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding IdentityReference}" />
                <DataGridCheckBoxColumn Header="Modify" Binding="{Binding CanModify}" IsReadOnly="True" />
                <DataGridCheckBoxColumn Header="Read/Execute" Binding="{Binding CanReadExecute}" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
    
    <TextBlock Grid.Row="2" Grid.ColumnSpan="4" Text="{Binding Res[loadingPermissions]}"
           Foreground="DarkRed"
           FontWeight="Bold"
           Margin="5"
           Visibility="{Binding IsLoadingPermissions, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>

</UserControl>
