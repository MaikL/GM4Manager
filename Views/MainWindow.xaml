<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GM4ManagerWPF.Localization"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="av" x:Class="GM4ManagerWPF.MainWindow"
        xmlns:uc="clr-namespace:GM4ManagerWPF"
        Title="GM4Manager" Height="800" Width="1200" DataContext="{Binding Source={x:Static local:ResourceService.Instance}}">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>
    <Window.WindowStartupLocation>CenterScreen</Window.WindowStartupLocation>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="{Binding [menuLanguages]}">
                <MenuItem Header="{Binding [menuEnglish]}" Click="SetLanguage" Tag="en"/>
                <MenuItem Header="{Binding [menuGerman]}" Click="SetLanguage" Tag="de"/>
            </MenuItem>
        </Menu>
        <TabControl>
            <TabItem x:Name="Manager" Header="{Binding [tabManager]}">
                <Grid Background="#FFE5E5E5">
                    <uc:Manager/>
                </Grid>
            </TabItem>
            <!--<TabItem Header="Explorer">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>-->
        </TabControl>
    </DockPanel>
    


</Window>